# Final Finance App

This project supports Supabase or local fallback using db.json. Admin credentials for local fallback:

- username: admin
- password: Admin@12345

Deploy to Vercel and set SUPABASE env vars if using Supabase.
